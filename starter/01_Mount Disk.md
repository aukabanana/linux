lsblk
	sudo mkdir -p /mnt/<path>
	sudo mount /dev/<nvm-path> /mnt/<path>
	ls /mnt/<external-path>
lsblk -f


Error: Permission has mission to read-only
sudo umount -l /disk
	sudo fsck.ext4 -f /disk (linux)
	sudo ntfsfix /disk (window)
	sudo fsck.vfat -a /disk (flashdrive)
sudo mount -a
